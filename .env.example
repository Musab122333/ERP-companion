# Phase 4 — API Configuration
# Save as .env in the same folder as main.py
# Use the SAME database credentials as Phase 2's .env

DB_HOST=localhost
DB_PORT=5432
DB_NAME=erp_warehouse
DB_USER=erp_sync
DB_PASSWORD=choose_a_strong_password_here
